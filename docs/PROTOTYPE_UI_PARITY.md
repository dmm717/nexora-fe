# BẢNG MA TRẬN ĐỐI SOÁT GIAO DIỆN NGUYÊN MẪU (PROTOTYPE UI PARITY MATRIX)
**Dự án:** Nexora AI Career Accelerator & Mock Interview Platform  
**Nhánh:** `feat/full-prototype-ui-parity`  
**Mục tiêu:** Đồng bộ toàn diện 100% giao diện, trải nghiệm (UI/UX), cấu trúc thông tin (IA) và hệ thống thẩm mỹ từ `qbao0111/nexora-prototype` sang `dmm717/nexora-fe`, đồng thời bảo toàn toàn bộ logic nghiệp vụ, API, React Query cache, xác thực và hợp đồng dữ liệu backend từ `qbao0111/nexora-backend`.

---

## 1. Tham chiếu SHA bắt đầu
- **Production FE Starting Main SHA:** `71bb0d6063fc537fe3d1b48bbcdf87d065a39d9c` (Merge PR #18 @ `d80d9266a8b8553f4a0cdaa608fb18916da833de`)
- **Prototype Reference SHA:** `46711620c8f184adcf3fab63b932823eade4a841`
- **Backend Reference SHA:** `f4e60b4e709ce34a552632a9957c2321121e5056`

---

## 2. Bảng ma trận đối soát chi tiết (Inventory of Authenticated Surfaces)

| Tuyến đường (Production Route) | Thành phần Production hiện tại | Màn hình / Component Prototype | Trạng thái đối soát (Parity Status) | UI/CSS cũ cần dỡ bỏ (Legacy UI to Remove) | Logic nghiệp vụ thực tế bảo toàn (Production Logic Preserved) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Authenticated App Shell** (`/(dashboard)/*`) | `DashboardLayout.tsx` + `DashboardLayout.module.css` (Thanh điều hướng ngang 9 mục cũ, wrapper `max-w-1200px`, menu tài khoản cũ) | `AuthenticatedMasterShell.tsx` + `AuthenticatedHeader.tsx` (5 mục chuẩn: Tổng quan, Phân tích CV, Phỏng vấn AI, Luyện tập, Năng lực, Bảng giá + Avatar menu) | `FULL_PARITY` | Toàn bộ `DashboardLayout.module.css`, menu ngang 9 nút cũ, nút "Bắt đầu phỏng vấn" cũ | `useCurrentUser`, `useCareerProfile`, `authApi.logout`, `planCode`, RBAC `isAdmin` router push sang `/admin` |
| **Focused Practice Shell** (`/interviews/[id]`, `/practice/star`, `/practice/scenarios/[slug]`) | `ProductFocusedSurface` bọc trong chrome dashboard cũ | `FocusedPracticeShell.tsx` + `FocusedPracticeHeader.tsx` (Thoát an toàn, Modal xác nhận rời phòng, step badge, theme tối tập trung) | `FULL_PARITY` | Chrome dashboard bên ngoài phòng phỏng vấn, các banner tràn viền cũ | Bộ đếm câu hỏi động, session status, điều hướng `/overview` an toàn |
| `/overview` (Tổng quan) | `src/app/(dashboard)/overview/page.tsx` + `DashboardPage.module.css` (Banner xanh, thẻ thống kê cũ, CareerProfileSection cũ) | `OverviewScreen.tsx` (`ProductPageHero`, 4 câu hỏi cốt lõi, Next Best Action spotlight, `EvidenceCard`, `InsightPanel`, `RadialScore`) | `FULL_PARITY` | `DashboardPage.module.css`, cấu trúc thẻ lưới cũ, banner xanh cũ | `useDashboardSummary`, `useProgressDashboard`, `useCareerProfile`, `useNextPracticeRecommendation`, tính toán điểm TB không làm tròn sai |
| `/career-goals` & `/account` (Hồ sơ nghề nghiệp & Thiết lập) | `CareerGoals.tsx`, `CareerProfileSection.tsx`, `AccountSettings.tsx` | `CareerProfileScreen.tsx` (Thẻ định danh, CV chính, mục tiêu tuyển dụng, radar năng lực, trạng thái onboarding) | `FULL_PARITY` | Thẻ cài đặt tài khoản rời rạc cũ, giao diện form cũ | `useCareerGoals`, `useCreateCareerGoal`, `useSetActiveCareerGoal`, `useUpdateCareerGoal`, `useResumes`, `useSetPrimaryResume` |
| `/resumes` (Quản lý CV) | `src/app/(dashboard)/resumes/page.tsx` (Bảng/thẻ CV cũ) | `CareerProfileScreen.tsx` (Khu vực danh sách CV, gắn cờ CV chính, upload trực tiếp, xóa CV) | `FULL_PARITY` | Layout bảng CV cũ | Quy trình Upload presign -> raw byte PUT -> POST resume, `useSetPrimaryResume`, `useDeleteResume` |
| `/resume-analyses` & `/cv-analysis` (Phân tích CV) | `src/app/(dashboard)/resume-analyses/page.tsx` (Trang tải lên và danh sách lịch sử phân tích cũ) | `CvAnalysisLaunchScreen.tsx` + `CvAnalysisProcessingScreen.tsx` (Hero đối chiếu, benchmark ngành vs targeted JD, thanh tiến trình phân tích) | `FULL_PARITY` | Khung phân tích cũ, form tab cũ | `cvAnalysisApi.analyze`, `runResumeAnalysisOperation`, chế độ `field_benchmark` & `job_targeted`, lịch sử phân tích CV |
| `/resume-analyses/[id]` (Kết quả phân tích CV) | `src/app/(dashboard)/resume-analyses/[id]/page.tsx` | `CvAnalysisResultScreen.tsx` (Thang điểm tổng quan, 6 trục radar, bảng bóc tách khoảng trống bằng chứng, khuyến nghị hành động) | `FULL_PARITY` | Thẻ kết quả phân tích cũ | `useResumeAnalysis`, hợp đồng `JobTargetedResult` và `FieldBenchmarkResult` |
| `/interviews/new` (Chuẩn bị phỏng vấn / Preflight) | `src/app/(dashboard)/interviews/new/page.tsx` (Wizard form cũ) | `InterviewPreflightScreen.tsx` (Hero nhịp phỏng vấn, kiểm tra mic động Web Audio API, chọn CV, nhập JD hoặc chọn mục tiêu, text-only mode) | `FULL_PARITY` | Giao diện form wizard cũ | `interviewApi.start`, `buildInterviewPreflightPayload`, tạo mới JD tức thời nếu có nội dung, sinh `idempotencyKey` |
| `/interviews/[id]` (Phòng phỏng vấn AI) | `src/app/(dashboard)/interviews/[id]/page.tsx` (Phòng phỏng vấn dark nhưng nằm trong chrome cũ) | `InterviewRoomScreen.tsx` + `FocusedPracticeShell.tsx` (Sân khấu Meet-style, Avatar AI trạng thái nói/nghe/nghĩ, Quick Coaching drawer, Web Speech STT/TTS, Checkpoint Q2/Q3) | `FULL_PARITY` | Khung navbar cũ bao bọc bên ngoài phòng phỏng vấn | `interviewApi.submitAnswer`, `interviewApi.continue`, bảo toàn tính bất biến câu trả lời, không chuyển trang mất phiên, kiểm tra quyền gói cước |
| `/interviews/[id]/report` (Báo cáo phỏng vấn) | `src/app/(dashboard)/interviews/[id]/report/page.tsx` | `InterviewReportScreen.tsx` (Tabs: Tổng quan 4 trục rubric, Chi tiết câu hỏi & transcript, Nhận xét STAR, Kế hoạch hành động, Luyện lại) | `FULL_PARITY` | Tabs cũ, bố cục in báo cáo cũ | `useInterviewReport`, `interviewApi.practiceAgain` (tạo phiên mới tham chiếu câu cũ), `interviewApi.retryReport` |
| `/practice` (Trung tâm luyện tập) | `src/app/(dashboard)/practice/page.tsx` + `PracticeHub.tsx` | `PracticeHubScreen.tsx` (Hero các đường luyện tập, phân nhóm 3 chế độ Phỏng vấn / Tình huống / STAR, Lịch sử luyện tập hợp nhất) | `FULL_PARITY` | Thẻ luyện tập dạng dashboard cũ | Lọc lịch sử hợp nhất từ `useInterviewsHistory`, `useScenarioAttempts`, `useStarAttempts`, điều hướng trực tiếp |
| `/practice/scenarios` & `/scenarios` (Kho tình huống) | `src/app/(dashboard)/scenarios/page.tsx` | `ScenarioLibraryScreen.tsx` (Bộ lọc danh mục, độ khó, năng lực, tìm kiếm, tình huống đề xuất) | `FULL_PARITY` | Danh sách tình huống cũ | `useScenarios`, `useScenarioAttempts`, kiểm tra quyền gói `entitlement.features.scenario.enabled` |
| `/practice/scenarios/[slug]` (Phòng giải quyết tình huống) | `src/app/(dashboard)/scenarios/[slug]/page.tsx` | `ScenarioRoomScreen.tsx` (Bối cảnh sự cố, trình biên soạn phương án, đánh giá tự động từ backend, so sánh delta điểm) | `FULL_PARITY` | Khung giải quyết tình huống cũ | `scenarioApi.createAttempt`, `scenarioApi.submitAttempt`, bảo toàn `completed attempts immutable`, `retry -> new attempt` |
| `/practice/star` & `/star-builder` (Phòng luyện STAR) | `src/app/(dashboard)/practice/star/page.tsx` | `StarPracticeRoomScreen.tsx` (Nhập câu trả lời tự nhiên duy nhất, AI bóc tách 4 thành phần S-T-A-R, thẻ STAR Evaluation chuẩn) | `FULL_PARITY` | Layout STAR Builder cũ | `starApi.submit`, bảo toàn ngữ nghĩa: `detected=false` -> `score=0` & `evidence=""`, câu trả lời gốc bất biến |
| `/skill-profile` (Hồ sơ kỹ năng) | `src/app/(dashboard)/skill-profile/page.tsx` + `SkillProfile.tsx` | `ProgressScreen.tsx` / `CareerProfileScreen.tsx` (Trực quan hóa năng lực theo phân nhóm động của backend, hiển thị số bằng chứng thực tế, tín hiệu khoảng trống) | `FULL_PARITY` | Thẻ kỹ năng tĩnh cũ | `useSkillProfile`, chuẩn hóa danh mục động từ máy chủ, không bịa đặt số lượng năng lực |
| `/learning-path` (Lộ trình học thích ứng) | `src/app/(dashboard)/learning-path/page.tsx` + `LearningPathView.tsx` | `LearningPathScreen.tsx` (Cấu trúc roadmap cột mốc, trạng thái hoạt động pending/completed/obsolete, chỉ pending có thể thao tác, tạo/làm mới lộ trình) | `FULL_PARITY` | Thẻ danh sách bài học tĩnh cũ | `useLearningPath`, `learningPathApi.generate`, `learningPathApi.refresh`, `learningPathApi.updateActivityStatus` |
| `/analytics` & `/progress` (Tiến bộ & Năng lực) | `src/app/(dashboard)/analytics/page.tsx` | `ProgressScreen.tsx` (Báo cáo hồ sơ năng lực thực chiến, `AnimatedRadialScore`, số năng lực đã kiểm chứng, danh sách bằng chứng) | `FULL_PARITY` | Giao diện Analytics cũ với biểu đồ tĩnh | `useProgressDashboard`, xử lý `null` readiness score thành "Chưa đủ dữ liệu đánh giá", bảo toàn `priorityGapCount` |
| `/billing` (Gói cước & Thanh toán) | `src/app/(dashboard)/billing/page.tsx` + `BillingView.tsx` | `BillingScreen.tsx` (Thẻ gói hiện tại, hạn mức phỏng vấn, hạn mức phân tích CV, lịch sử đơn hàng SePay) | `FULL_PARITY` | Thẻ quản lý gói cũ | `billingApi.getEntitlement`, `billingApi.getOrders`, `billingApi.createOrder` |
| `/pricing` (Bảng giá dịch vụ) | `src/app/pricing/page.tsx` + `PricingCards.tsx` | `PricingScreen.tsx` (Hero các nấc mở rộng, Ma trận 4 gói Free/Basic/Weekly/Pro, Contextual Upgrade banner khi kết thúc Q3) | `FULL_PARITY` | Giao diện bảng giá cũ | `plansApi.getPlans`, luồng thanh toán SePay, `returnTo` redirect |
